# UI Style Guide

Status: not yet written. Owned by the UI session once one exists — this
file should be rewritten (not just appended to) as visual decisions land.

Nothing to record yet. First UI session should draft an initial pass here
during Sprint 0 or Sprint 1: color palette, typography, HUD layout
conventions, and how tower/enemy readability is handled at high entity
counts (this matters more than usual for this project — see the
performance requirement in `GAME_DESIGN.md`; UI needs to stay legible even
when the board is dense).
